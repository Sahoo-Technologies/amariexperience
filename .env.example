# Amari Experience — Environment Variables
# Copy this file to .env and fill in your actual credentials.
# NEVER commit .env with real values.

# Required: Primary Neon PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Optional: Supabase Postgres fallback (used if DATABASE_URL is unavailable)
SUPABASE_DB_URL=postgresql://user:password@host/dbname?sslmode=require

# Required: Secret for signing JWT session cookies (min 32 chars, generate a random UUID)
JWT_SECRET=replace-with-a-secure-random-string-min-32-chars

# Optional: Google Gemini API key for AI planning features
GEMINI_API_KEY=

# Optional: Frontend API base URL (leave empty if same origin / Vercel)
VITE_API_BASE=
